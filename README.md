# ПРАКТИЧЕСКАЯ РАБОТА «ФУНКЦИИ В JS. FUNCTION DECLARATION, EXPRESSION, СТРЕЛОЧНЫЕ ФУНКЦИИ. ОБЛАСТЬ ВИДИМОСТИ»

## Цель работы:
- научиться выбирать правильный тип функции для задачи, понимать области видимости переменных (Scope), освоить замыкания (Closures), разграничивать доступ к переменным, применять функции для решения практических задач и подготовиться к вопросам нп Junior-собеседовании

## Используемые технологии в работе
### **HTML**
**_В работе использована современная разметка для создания интерфейса калькуляторов:_**
1. Теги секций (`<section>`, `<div>`): использованы для логического разделения заданий на карточки (`card`), что позволяет изолировать код каждого блока.
2. Элементы форм:
+ `<input type="number">`— для ввода числовых параметров (например, вес, рост, возраст);
+ `<select>` — для создания интерактивных выпадающих списков;
+ `<button>` — для инициализации вызова функций.
3. Атрибут `title`: Применен для создания всплывающих подсказок (`tooltips`) с информацией о допустимых диапазонах ввода.

### **CSS**
**_Для стилизации приложения применены следующие технологии:_**
1. CSS Variables (Переменные): позволили реализовать динамическую смену тем (светлая/темная) через изменение значений цветов в одном месте (`:root`).
2. Flexbox Layout: обеспечил вертикальное расположение полей («один под другим») и выравнивание кнопок.
3. Transitions (Переходы): использованы для плавного изменения цвета рамки при валидации и плавного появления плашек с результатом.
4. Визуальная валидация: реализована через динамическую смену классов `.success` (зеленая рамка) и `.error` (красная рамка), что дает мгновенный фидбек пользователю.

### **JS**
**_В работе продемонстрированы основные способы создания функций:_**
1. Function Declaration (Объявление функции)- пример: `function calculateBMI(...)`.
_Технология:_ использован Hoisting (всплытие), что позволяет вызывать эти функции в коде выше места их фактического написания.
2. Function Expression (Функциональное выражение) - пример: `const toLbs = function(...)`.
_Технология:_ функция создается как значение переменной. У таких функций нет всплытия, поэтому их можно вызывать только после объявления.
3. Arrow Functions (Стрелочные функции) - пример: `const cmToFt = (cm) => ...`.
_Технология:_ использован современный короткий синтаксис (`ES6`). В Задании 4 они применены для лаконичных вычислений без использования ключевого слова `return` в одну строку.
4. Closures (Замыкания):
_Технология:_ реализована функция-валидатор, которая «запоминает» минимальные и максимальные границы диапазона из своей внешней области видимости.
5. Scope (Область видимости):
Global Scope: глобальные константы (например, коэффициенты) доступны всем функциям через лексическое окружение.
Function Scope: локальные переменные внутри расчетов защищены от изменений извне и не «загрязняют» глобальную область.
> **_Реализованные функции по уровням:_**
1. _Базовый:_ расчет ИМТ и конвертация веса с проверкой на положительные числа.
2. _Junior:_ расчет нормы калорий (BMR) с использованием глобальных констант и сложная конвертация роста в формат `5'9"`.
3. _PRO:_ валидация данных через механизм замыканий и демонстрация уровней видимости переменных в консоли.
Для корректной работы «живой» валидации использован глобальный обработчик событий `addEventListener('input', ...)`, который мгновенно проверяет данные согласно правила: «одна функция – одно действие».

### **GitHub**
**_Ключевые технологии GitHab:_**
+ хостинг пакетов GitGitHub Actions Wikipedia для CI/CDGitHub Packages Wikipedia (поддержка npm, Docker, Maven, NuGet);
+ автодополнение кода GitHub Copilot Wikipedia (ИИ);
+ облачные среды разработки GitHub Codespaces Wikipedia/

## Созданные в работе функции
Данная работа представляет собой страницу _«Калькулятор здоровья и фитнеса»_, объединяющая в себе несколько инструментов для расчета параметров тела и конвертации единиц измерения.
### **_В работе реализованы следующие функции (согласно принципу Single Responsibility):_**
1. `calculateBMI (weight, height)` (Function Declaration) – рассчитывает индекс массы тела. Выбрана из-за поддержки `hoisting`, что позволяет вызывать её в любом месте скрипта.
2. `kgToLbs` / `lbsToKg` (Function Expression) – конвертеры веса. Выбраны для обеспечения строгого порядка кода (недоступны до объявления).
3. `calculateBMR (weight, height, age, gender)` – рассчитывает базальный метаболизм по формуле Миффлина-Сан Жеора. Демонстрирует работу с `Lexical Environment` (использование внешней константы `BMR_COEFFICIENT`).
4. `cmToFtIn` / `ftInToCm` (Arrow Functions) – краткие функции в одну строку для конвертации роста. Выбраны за лаконичный синтаксис.
5. Функции-обработчики (`handleBMI`, `handleBMR` и др.) – отвечают за сбор данных с экрана, их «валидацию» и вывод результата пользователю.

## Ответы на контрольные вопросы (вопросы для интервью)
**1. _Что произойдет, если в функции нет оператора return?_**
  Ключевое слово `return` отправляет результат работы функции обратно в место вызова. Если в теле функции оператор return отсутствует, функция выполнит весь заложенный в неё код и по умолчанию вернет значение `undefined`.
**2. _Почему стрелочные функции нельзя вызвать до ее объявления?_**
  Стрелочная функция (Arrow Function) представляет собой современный синтаксис функционального выражения. Как и в случае с Function Expression, для них нет hoisting (всплытия). Поскольку функция создается как значение переменной (обычно `const`), она инициализируется только в момент достижения интерпретатором строки кода с её объявлением.
**3. _Что такое «Замыкание» (Closure) простыми словами?_**
  Замыкание – это ситуация, когда внутренняя функция «запоминает» переменные из своей внешней области видимости. Она продолжает «помнить» и иметь доступ к этим переменным даже после того, как внешняя функция завершила свое выполнение. В нашей работе это использовано в _задании 5_ для создания валидатора с фиксированными границами диапазона.
**4. _Зачем нужен принцип «одна функция – одна задача» (Single Responsiblity)?_**
  Согласно теории и практики, функция должна делать только то, что явно подразумевается её названием. Это помогает лучше структурировать код, избавляться от запутанных частей и делать функции независимыми. Например, функция проверки данных (`validate`) должна только проверять их, а не выводить сообщения об ошибках.
**5. _Чем отличается Function Declaration от Function Expression?_**
  Главное отличие в `hoisting` (всплытии). Declaration создается при запуске скрипта и доступна везде. Expression создается только тогда, когда поток выполнения доходит до неё.
**6. _Что такое hoisting для функции?_**
  `Hoisting` – это механизм в JavaScript, который «поднимает» объявления функций (Function Declaration) наверх в начало области видимости. Это происходит потому, что JS «читает» весь код перед выполнением и находит все FD, позволяя вызывать их в любом месте программы
**7. _Почему стрелочные функции нельзя вызвать до объявления?_**
  Они всегда являются анонимными и обычно присваиваются переменным (`const` или `let`). Переменные не поддерживают hoisting в той же мере, что и Function Declaration, поэтому вызов до строки инициализации приведет к ошибке.
**8. _Что такое лексическое окружение?_**
  Это внутренний механизм JS, который хранит данные функции и ссылку на внешнее окружение. Именно благодаря ему функция "знает", какие переменные ей доступны снаружи.
**9. _Почему важно комментировать выбор типа функции?_**
  Выбор типа функции (`FD`, `FE` или `AF`) в JavaScript напрямую влияет на архитектуру: использование Function Declaration позволяет гибко располагать код, а Arrow Functions обеспечивают лаконичность для простых операций. Комментирование выбора показывает понимание таких нюансов, как hoisting и контекст выполнения, что критически важно для командной разработки и поддержки кода.


